# ASF Formulas

The canonical formula definitions have moved to:

**[methodology/core.md](../methodology/core.md)**

That document contains:
- The ALS formula with exact weights
- The CRT formula
- All dimension definitions
- The adaptation gap model

`docs/formulas.md` is kept as a redirect to avoid broken links.
The content previously here is a subset of what is now in `methodology/core.md`.
